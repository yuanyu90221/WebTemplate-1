1. 
	當 table schema 改變, 
	
	先把 
		src/main/java -> com.exfantasy.template.mybatis
	底下的東西全殺掉
	
2. 

	到 config/mybatis_generator_config.xml 上面按右鍵 Generate

3. 
	到 src/main/java -> com.exfantasy.template.mybatis.mapper
	
	每隻 Mapper 加上 annotation @Mapper

4.
	到 src/main/java -> com.exfantasy.template.mybatis.model
	
	每隻 Value Object 加上欄位註解
	
----------------------------------------------------------------

mybatis_generator_config.xml

其中 javaClientGenerator 的  type 改為 "ANNOTATEDMAPPER",

因使用 XMLMAPPER, 

用 gradle 包出來的 jar 檔啟動會有問題, 暫時先改用 ANNOTATEDMAPPER